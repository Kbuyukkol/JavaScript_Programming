let x= 6;

x += 4;

console.log(x);


let one=1;

let one_again= 1;

let one_string ='1';

console.log(one==one_again); //true
console.log(one==one_string); //true == to check for value 
console.log(one===one_string); //false === to check for both value and dataType